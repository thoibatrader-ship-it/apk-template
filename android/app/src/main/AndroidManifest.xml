<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <!-- Always kept: normal-protection permission, no runtime prompt, never
         flagged by store review. Removing it risks silently breaking apps
         that quietly load an external font/CDN/API. Not wrapped in a
         PERM marker on purpose — the dashboard never trims this. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- PERM:CAMERA:START -->
    <!-- Camera -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <!-- PERM:CAMERA:END -->

    <!-- PERM:STORAGE:START -->
    <!-- Storage / Gallery (legacy, Android 12 and below) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <!-- Storage / Gallery (granular media, Android 13+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- PERM:STORAGE:END -->

    <!-- PERM:LOCATION:START -->
    <!-- GPS / Location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- PERM:LOCATION:END -->

    <!-- PERM:MIC:START -->
    <!-- Microphone -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- PERM:MIC:END -->

    <!-- PERM:NOTIFICATIONS:START -->
    <!-- Notifications (Android 13+ requires runtime prompt) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- PERM:NOTIFICATIONS:END -->

    <!-- PERM:NOTIFICATIONS:OFFONLY:START -->
    <!-- @capacitor/local-notifications ships its own AndroidManifest.xml that
         unconditionally declares these (for reboot-restore of scheduled
         alarms), and Gradle's manifest merger pulls them in regardless of
         what's written above — that's why "Alarms & reminders" / boot-receiver
         access can show up on an app that never touches notifications.
         This block only stays in the file (and cancels them) when the
         NOTIFICATIONS group above was trimmed out; if NOTIFICATIONS is kept,
         this whole block is removed too so exact alarms keep working. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" tools:node="remove" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" tools:node="remove" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" tools:node="remove" />
    <uses-permission android:name="android.permission.WAKE_LOCK" tools:node="remove" />
    <!-- PERM:NOTIFICATIONS:OFFONLY:END -->

    <!-- PERM:VIBRATE:START -->
    <!-- Vibrate (no runtime prompt needed, included for haptics plugin) -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- PERM:VIBRATE:END -->

    <!--
      Optional / less-common permissions — commented out by default to keep the
      permission surface minimal (fewer runtime prompts = fewer places for a
      per-user app to break). Uncomment per-app in the generated repo if the
      user's HTML/JS actually needs them. NOT wired to auto-detection: these
      plugins (@capacitor-community/contacts, bluetooth-le, calendar) aren't
      installed in package.json by default, so turning the permission on
      alone wouldn't make the feature work — needs manual setup either way.

      <uses-permission android:name="android.permission.READ_CONTACTS" />
      <uses-permission android:name="android.permission.WRITE_CONTACTS" />
      <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
      <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
      <uses-permission android:name="android.permission.READ_CALENDAR" />
      <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    -->
</manifest>
