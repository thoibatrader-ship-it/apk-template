import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dashboard.placeholder',
  appName: 'App Placeholder',
  webDir: 'www'
};

export default config;
